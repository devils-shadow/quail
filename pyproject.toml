[project]
name = "quail"
version = "0.1.0"
description = "Self-hosted, receive-only mail sink"
readme = "README.md"
requires-python = ">=3.11"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
